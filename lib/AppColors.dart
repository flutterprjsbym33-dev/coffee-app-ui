import 'dart:ui';

class AppColors {
  // Note: Fixed invalid color codes from the original XML
  static const black = Color(0xFF000000);  // Fixed from #FFG00000
  static const white = Color(0xFFFFFFFF);  // Fixed from #FFFFFFFFF

  static const orange = Color(0xFFF0845D);
  static const cream = Color(0xFFEFDEC9);
  static const lightCream = Color(0xFFFCFDE2);  // Changed to camelCase
  static const darkGrey = Color(0xFF4E4E4E);
  static const grey = Color(0xFF80808D);
  static const darkBrown = Color(0xFF220C01);  // Fixed from #220CC01
}